<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="asteroides.example.org.asteroides">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/shini_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/shini_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_acerda_de"
            android:label="Acerca de ..."/>
        <activity android:name=".PreferenciasActivity"/>
        <activity android:name=".Puntuaciones"/>
        <activity android:name=".Juego"/>
    </application>

</manifest>